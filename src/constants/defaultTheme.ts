/**
 * @description Declares the default theme and type for the library.
 * As default we use an empty object
 * @author Carlos Balladares
 */

type Theme_T = {
  [key: string]: any;
};

const defaultTheme: Theme_T = {};
export {defaultTheme, Theme_T};
