import {themeCreator} from './utils';
const {
  ThemeContext,
  ThemeProvider,
  useTheme,
  withTheme,
  makeUseStyles,
} = themeCreator({});
export {
  ThemeContext,
  ThemeProvider,
  useTheme,
  withTheme,
  makeUseStyles,
  themeCreator,
};
